.DS_Store
.vscode
/.build
/build
/dist
xcuserdata/
DerivedData/
.swiftpm/
*.xcodeproj
.netrc

# Sparkle EdDSA private key. This repo is public, and this key authorizes code to
# run on every user's Mac — it must never be committed, encrypted or not.
*-key.txt
*-key.txt.gpg
